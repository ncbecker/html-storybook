import { createTrackElement } from "./mainplayer";

export default {
  title: "Components/Main Player",
  parameters: { layout: "centered" },
};

export const mainPlayer = () => createMainPlayerElement();
